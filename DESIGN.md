# 최우열 개인 홈페이지 디자인 노트

## 방향

이름과 자기소개가 먼저 읽히는, 깔끔하고 차분한 개인 홈페이지를 목표로 했다.
기존에 요청한 취향인 ‘깔끔하고 읽기 좋게, 휴대폰에서도 잘 보이게’를 기준으로
큰 제목, 충분한 여백, 절제된 색상을 선택했다. 사진 대신 이름과 글이 중심이 되는 구성을 사용했다.

## 색상

| 용도 | 색상 | 선택 이유 |
| --- | --- | --- |
| 주요 글자·프로필 메모 | 네이비 `#17243b` | 차분한 분위기와 선명한 글자 대비 |
| 배경 | 밝은 회색 `#f7f9fc` | 흰색 콘텐츠 영역과 자연스럽게 구분 |
| 보조 글자 | 회청색 `#526078` | 본문을 편하게 읽으면서 제목과 구분 |
| 번호·강조 글자 | 짙은 오렌지 `#ad3c16` | 네이비와 대비되는 작은 포인트 |
| 프로필 메모 윗선 | 오렌지 `#ed744a` | 화면에 개인적인 인상을 더하는 장식 |

## 글자와 배치

- 이름은 화면 크기에 따라 약 56~112px로 조절해 페이지의 중심으로 배치했다.
- 본문은 기본 16px, 줄 간격은 1.7로 설정했다.
- 한글 시스템 글꼴을 사용하며 외부 글꼴 다운로드 없이도 읽을 수 있다.
- 소개, 관심사, 연락처에 01~03 번호를 붙여 읽는 순서를 분명하게 했다.
- 넓은 화면에서는 소개와 관심사를 나란히, 연락처는 아래 한 줄로 배치했다.
- 위쪽에는 현재 배우는 내용을 담은 네이비 메모를 넣었다.
- 기존 원형 이니셜, 둥근 카드, 파란색 태그에서 큰 이름 제목, 작은 모서리, 오렌지 포인트로 변경했다.

## 휴대폰과 접근성

- 640px 이하 화면에서는 모든 영역을 한 열로 배치한다.
- 메뉴와 태그는 공간이 부족하면 줄바꿈되어 가로 넘침을 줄인다.
- 키보드로 이동할 때 링크의 테두리가 표시된다.
- 본문 바로가기, 한국어 문서 설정, 의미 있는 제목과 탐색 영역을 제공한다.
- 기기의 움직임 줄이기 설정을 선택하면 부드러운 스크롤을 끈다.

## 내용과 구현

이름은 최우열, 소속은 서울대학교 첨단융합학부로 명시했다.
소개에는 프로그래밍을 시작하고 개인 웹페이지를 만드는 현재 상황을 담았다.
별도의 이메일이나 전화번호는 제공되지 않아 연락처 영역에는 실제 GitHub 프로필을 연결했다.

HTML과 CSS를 `index.html` 하나에 작성했다. 서버, 유료 서비스, 추가 설치,
외부 스크립트 없이 GitHub Pages에서 제공할 수 있는 정적 페이지다.

## 수정 방법

- 글과 링크: `index.html`의 `<body>` 부분에서 수정한다.
- 전체 색상: `<style>` 안의 `:root` 값을 바꾼다.
- 모바일 배치: `@media (max-width:640px)` 부분에서 조정한다.
- 디자인을 수정하면 이 문서에도 선택 이유를 함께 기록한다.
